version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.x
  build:
    commands:
      - echo "Installing Python 3"
      - sudo apt-get update -y
      - sudo apt-get install -y python3 python3-pip
artifacts:
  files:
    - '**/*'
